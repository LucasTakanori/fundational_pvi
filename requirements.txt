# Core runtime dependencies for the PVI pipeline + foundation model.
# Install with:  pip install -r requirements.txt
# (CPU torch is fine for the smoke test; install a CUDA build for real training.)

torch
numpy
scipy
scikit-learn
pandas
h5py
PyYAML
POT          # imported as `ot`
tqdm
matplotlib   # analysis plots (src/analysis/budget_curves.py); import is guarded
datasets     # HF datasets, used by the parquet cache loader (src/pipeline/pvi_cache.py)
pyarrow      # parquet read/write for the pvi cache

# --- Dev / test ---
pytest

# --- Optional ---
# geomloss     # only src/models/quality_evaluator.py (import is guarded)
# mambapy      # only src/models/s4_models.py (Mamba/S4, WIP; import is guarded)
